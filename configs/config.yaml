server:
  auth_port: 8001
  queue_port: 8002
  booking_port: 8003
  websocket_port: 8004

database:
  host: localhost
  port: 5432
  user: ivan
  password: Lost2409
  dbname: main-ste
  sslmode: disable
  max_connections: 100
  min_connections: 10

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  pool_size: 10

jwt:
  secret_key: "your-secret-key-change-this-in-production"
  expiration_hours: 24

cors:
  allowed_origins:
    - "http://localhost:3000"
    - "http://localhost:5173"
  allowed_methods:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "OPTIONS"
  allowed_headers:
    - "Content-Type"
    - "Authorization"
